import 'package:flutter/material.dart';
import 'package:ticketify/common/widgets/app_bar.dart';

class HostEventEntryPage extends StatelessWidget {
  const HostEventEntryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: buildAppBar(),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
